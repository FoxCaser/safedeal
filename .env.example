# Optional Google Web Risk key (requires Google Cloud billing)
GOOGLE_WEB_RISK_API_KEY=

# Optional PhishTank app key. SafeDeal can use PhishTank without it,
# but PhishTank applies a much lower request limit when no key is supplied.
PHISHTANK_APP_KEY=

# Optional PostgreSQL connection string for persistent community reports
DATABASE_URL=

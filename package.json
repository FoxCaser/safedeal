{
  "name": "safedeal",
  "version": "0.1.0",
  "private": true,
  "description": "SafeDeal — anti-fraud checks for sellers, jobs, links, contacts, text and screenshots.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20 <25"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0"
  }
}
